apiVersion: apps/v1
kind: Deployment
metadata:
  name: website-pr-10
  labels:
    app.kubernetes.io/name: website-preview
    app.kubernetes.io/instance: pr-10
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: website-preview
      app.kubernetes.io/instance: pr-10
  template:
    metadata:
      labels:
        app.kubernetes.io/name: website-preview
        app.kubernetes.io/instance: pr-10
    spec:
      containers:
        - name: website
          image: ghcr.io/coppersoft-dev/website:pr-10-b1f97bfa32416cf8ae17fc82f4e8e28359d379e6
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              protocol: TCP
              containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            privileged: false
            readOnlyRootFilesystem: true
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 50m
              memory: 32Mi
          volumeMounts:
          - name: cache
            mountPath: /var/cache/nginx
          - name: tmp
            mountPath: /tmp
      volumes: 
      - name: cache
        emptyDir: {}
      - name: tmp
        emptyDir: {} 
      securityContext:
        runAsNonRoot: true

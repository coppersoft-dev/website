apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: pr-11
    app.kubernetes.io/name: website-preview
  name: website-pr-11
spec:
  selector:
    app.kubernetes.io/name: website-preview
    app.kubernetes.io/instance: pr-11
  ports:
    - port: 80
      protocol: TCP
      targetPort: http
      name: http
